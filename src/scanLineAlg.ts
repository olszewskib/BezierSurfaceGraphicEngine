import type { Point } from "./point";

export function getScanLines(vertices: Point[]) {

    // i need to store edges first before sorting the vertices how else would i know them
    //vertices.sort((p1: Point, p2: Point) => p1.y - p2.y);

    // Todo: Transform the vertex list into an edge list with shortened edges

    // Todo: Remove horizontal edges from ET[]

    //

}